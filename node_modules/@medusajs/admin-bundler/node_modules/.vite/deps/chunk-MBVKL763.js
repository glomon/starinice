import {
  useDeleteProductCategory
} from "./chunk-2R2KQPMU.js";
import {
  useTranslation
} from "./chunk-FSQ44SVG.js";
import {
  useNavigate
} from "./chunk-2WRJVAK6.js";
import {
  toast,
  usePrompt
} from "./chunk-CVCKA5P5.js";

// node_modules/@medusajs/dashboard/dist/chunk-R7V6PQES.mjs
var useDeleteProductCategoryAction = (category) => {
  const { t } = useTranslation();
  const navigate = useNavigate();
  const prompt = usePrompt();
  const { mutateAsync } = useDeleteProductCategory(category.id);
  const handleDelete = async () => {
    const res = await prompt({
      title: t("general.areYouSure"),
      description: t("categories.delete.confirmation", {
        name: category.name
      }),
      confirmText: t("actions.delete"),
      cancelText: t("actions.cancel")
    });
    if (!res) {
      return;
    }
    await mutateAsync(void 0, {
      onSuccess: () => {
        toast.success(
          t("categories.delete.successToast", {
            name: category.name
          })
        );
        navigate("/categories", {
          replace: true
        });
      },
      onError: (e) => {
        toast.error(e.message);
      }
    });
  };
  return handleDelete;
};

export {
  useDeleteProductCategoryAction
};
//# sourceMappingURL=chunk-MBVKL763.js.map
